import { type ReactElement, type ReactNode } from "react";
export declare function Card(props: {
    title: string;
    description?: string;
    children?: ReactNode;
    className?: string;
}): ReactElement;
